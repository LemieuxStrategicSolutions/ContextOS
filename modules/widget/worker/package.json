{
  "name": "os-widget-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that serves the dashboard JSON for the phone widget",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
